#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='adaptiveconventions',
      version='0.0.1',
      description='Adaptive-Conventions',
      author='',
      author_email='',
      packages=find_packages(),
      include_package_data=True,
      install_requires=[
      ],
      )
